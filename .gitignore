# dependencies
node_modules

# build output
dist
dist-ssr

# local env
.env
.env.local
.env.*.local

# editor / OS
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Design handover — generated self-contained HTML snapshots (large, regenerable).
/design-handover/
/flatten.mjs

# Source materials & design references — kept locally, not part of the built site.
# (The 30 MB master PNG, the strategy brief, and the original mockup live on disk;
#  the shipping asset is the optimized public/hero-poster.jpg.)
/*.docx
/*.png
/20.06.26 sacred_light_symphony_hero_v2.html
/screenshots/
